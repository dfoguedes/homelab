deployment:
  replicas: 1

service:
  type: NodePort
  nodePorts:
    http: 30080
    https: 30443

ingressRoute:
  dashboard:
    enabled: true

additionalArguments:
  - "--log.level=INFO"
  - "--api-dashboard=true"
  - "--api.insecure=true"

logs:
  general:
    level: INFO
